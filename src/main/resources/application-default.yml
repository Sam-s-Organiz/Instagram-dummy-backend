server:
  port: 8081

spring:
  # DataSource configuration
  datasource:
    url: jdbc:mysql://localhost:3306/DummyInsta?serverTimezone=UTC
    username: root
    password: WelCome!23$5
    driver-class-name: com.mysql.cj.jdbc.Driver

    # Hikari connection pool settings
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 120000
      connection-timeout: 20000
